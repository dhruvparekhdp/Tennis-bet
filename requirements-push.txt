# Dependencies for the LAPTOP push client (push_client.py) — NOT needed on Render.
# These run the local Flashscore + Parimatch scrapers that forward data to the server.
#
# Install:
#   pip install -r requirements-push.txt
#   playwright install chromium      # only if using --parimatch
#
# The core scraping (Flashscore) only needs httpx + structlog, already in requirements.txt.
# Playwright is only required for the Parimatch (--parimatch) collector.
httpx>=0.27
structlog>=24.0
playwright>=1.44
